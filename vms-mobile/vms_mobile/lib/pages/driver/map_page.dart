import 'package:flutter/material.dart';

class TaskMap extends StatelessWidget {
  const TaskMap({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text("Map")),
    );
  }
}
